
const Fetch = async ({ routeParams, _isClient, ctx }) => {
  // const res = await (await fetch('/api/index')).json();
  // if (!_isClient) {
  //   console.log(await ctx.apiService.index());
  // } else {
  //   console.log('client fetch');
  // }
  const res = 123;
  return res;
};

export default Fetch